import {Requests, Round} from '../src/play'

describe('play function', () => {
    beforeEach(() => {
    })

    describe('when', () => {
        it('does a thing', () => {
            expect(true).toEqual(true);
        })
    })
})